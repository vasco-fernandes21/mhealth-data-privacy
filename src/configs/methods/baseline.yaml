# ================================================================
# BASELINE - Standard Training
# ================================================================
# Baseline method uses dataset-specific configurations.
# All training parameters are defined in the dataset config files.
method: "baseline"
description: "Standard training without privacy"